using System.Reflection;
using System.Reflection.Emit;

namespace ExpandedAiFramework
{
    public class PackManagerSettings : JsonModSettings
    {
        public PackManagerSettings(string path) : base(path) { }
    }
}